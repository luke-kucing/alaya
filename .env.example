# Required: path to your zk notebook root
ZK_NOTEBOOK_DIR=~/notes

# External providers (configure any combination)
# Each provider enables pull_external / push_external for that platform.
# Issue CRUD (list, close, update) is handled by Claude via native CLI.

# GitLab (requires glab CLI: brew install glab)
# GITLAB_PROJECT=myorg/myrepo
# GITLAB_DEFAULT_LABELS=

# GitHub (requires gh CLI: brew install gh)
# GITHUB_REPO=owner/repo
# GITHUB_DEFAULT_LABELS=

# Outline (team knowledge base)
# OUTLINE_URL=https://outline.mycompany.com
# OUTLINE_API_KEY=
