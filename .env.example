# Required: path to your zk notebook root
ZK_NOTEBOOK_DIR=~/notes

# Issue tracking backend: "gitlab" or "github" (auto-detected if only one is set)
# ISSUE_BACKEND=

# GitLab integration (requires glab CLI: brew install glab)
# GITLAB_PROJECT=myorg/myrepo
# GITLAB_DEFAULT_LABELS=

# GitHub integration (requires gh CLI: brew install gh)
# GITHUB_REPO=owner/repo
# GITHUB_DEFAULT_LABELS=
