# Required: path to your zk notebook root
ZK_NOTEBOOK_DIR=~/notes

# Optional: GitLab project path (e.g. "myorg/myrepo") for GitLab integration
# GITLAB_PROJECT=

# Optional: default labels to apply to created GitLab issues (comma-separated)
# GITLAB_DEFAULT_LABELS=
